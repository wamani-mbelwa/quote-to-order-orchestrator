import dotenv from 'dotenv'; dotenv.config();
export const config={ port:Number(process.env.PORT||4000), jwtSecret:process.env.JWT_SECRET||'dev-secret', pg:{host:process.env.PG_HOST||'localhost',port:Number(process.env.PG_PORT||5432),user:process.env.PG_USER||'postgres',password:process.env.PG_PASSWORD||'postgres',database:process.env.PG_DATABASE||'quoteorder'}, redis:{host:process.env.REDIS_HOST||'localhost',port:Number(process.env.REDIS_PORT||6379)}, aws:{region:process.env.AWS_REGION||'us-east-1',endpoint:process.env.AWS_ENDPOINT}, dynamo:{endpoint:process.env.DYNAMO_ENDPOINT, table:process.env.DYNAMO_TABLE||'jobs_state'}, sqs:{queueUrl:process.env.SQS_QUEUE_URL||'http://localhost:4566/000000000000/quote-events'}, eventBridge:{busName:process.env.EVENT_BUS_NAME||'local-bus'} };
